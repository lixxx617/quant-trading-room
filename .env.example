# 複製這個檔案為 .env，並填入你自己的憑證。
# .env 這個檔名請加入 .gitignore，不要把真實金鑰commit進版控。
# 兩個管道都可以只填其中一個；留空的管道會被 notifier.py 自動跳過。

# ── Telegram Bot ──────────────────────────────────────────────
# 1. 在 Telegram 搜尋 @BotFather，傳送 /newbot 建立一個新 bot，取得 Token
# 2. 把這個 bot 加為好友，隨便傳一則訊息給它（或把它加進群組）
# 3. 瀏覽器打開 https://api.telegram.org/bot<你的TOKEN>/getUpdates
#    在回傳的 JSON 裡找 "chat":{"id": ...} 這個數字，就是 TELEGRAM_CHAT_ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── LINE（注意：LINE Notify 已於 2025-03-31 終止服務） ──────────
# 舊版 LINE Notify 已經無法使用，這裡改用 LINE 官方帳號的
# Messaging API push message，設定方式跟以前的 LINE Notify 不一樣：
# 1. 到 LINE Developers Console (https://developers.line.biz/console/)
#    建立一個 Provider，再建立一個 Messaging API Channel
# 2. 在該 Channel 的設定頁「Messaging API」分頁，按「Issue」
#    產生一組 Channel access token (long-lived)
# 3. 用 LINE App 掃描該 Channel 的 QR Code，把這個官方帳號加為好友
# 4. 在 Channel 設定頁的「Basic settings」可以找到你自己的 userId
#    （或用 webhook 接收事件取得 userId）
LINE_CHANNEL_ACCESS_TOKEN=
LINE_USER_ID=
